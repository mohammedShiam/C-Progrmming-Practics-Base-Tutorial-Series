#include <stdio.h>

int main()
{
    // TODO: C program to print a sentence.

    // printf("Year - 2021\n");

    // TODO: C program to add one or two integers entered by the User.

    // int intVar;
    // float floatVar;
    // char ch;

    // printf("Enter a intiger, float and char type data\n");
    // scanf("%d %f %c", &intVar, &floatVar, &ch);

    // printf("Intiger number = %d\tFloat number = %f\tChar is = %c\n",intVar,  floatVar, ch);

    // TODO: C program to multiply two floating-point numbers..

    // int intVar;
    // float floatVar;
    // float floatVar01;

    // printf("Enter two float type data");
    // scanf("%f %f", &floatVar, &floatVar01);

    // printf("Ente an intiger number\n");
    // scanf("%d", &intVar);

    // float mal =  floatVar * floatVar01;
    // int sum = mal + intVar;

    // printf("The maltiply of %f * %f = %f\n",floatVar, floatVar01, mal);
    // printf("Sum of %f and %d is %d\n", mal, intVar, sum);

    // TODO: C program to find quotient and remainder of Two Integers.

    // int num1, num2, div, rem;

    // printf("Enter 2 intiger number\n");
    // scanf("%d %d", &num1, &num2);

    // div = num1/num2;
    // rem = num1%num2;

    // printf("Quotient = %d\t Remainder = %d\n",div,rem );
    

    // TODO: Program to find the size of int, float, double and char.

    // printf("Size of intiger variable is %zu\n", sizeof(long long int));

    // double intigerType;

    // printf("The type of data intigerType is %zu\n", sizeof(intigerType));


    return 0;
}